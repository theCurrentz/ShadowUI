--[[
  Purpose: Shipped class defaults for WARLOCK (pet bar uses base placement).
  Deps: ShadowUI addon table
  Public: populates ShadowUI.Defaults.classes.WARLOCK
]]

local Addon = LibStub("AceAddon-3.0"):GetAddon("ShadowUI")

Addon.Defaults.classes.WARLOCK = {
  layout = {},
  keybinds = {},
}
