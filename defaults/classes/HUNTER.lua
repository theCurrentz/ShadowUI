--[[
  Purpose: Shipped class defaults for HUNTER (pet bar uses base placement).
  Deps: ShadowUI addon table
  Public: populates ShadowUI.Defaults.classes.HUNTER
]]

local Addon = LibStub("AceAddon-3.0"):GetAddon("ShadowUI")

Addon.Defaults.classes.HUNTER = {
  layout = {},
  keybinds = {},
}
